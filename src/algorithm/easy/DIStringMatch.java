package algorithm.easy;public class DIStringMatch {    public int[] diStringMatch(String S) {        int len = S.length();        int[] A = new int[len + 1];        int s = 0;        int e = len;        for (int i = 0; i < len; i++) {            if (S.charAt(i) == 'D') {                A[i] = e--;            } else {                A[i] = s++;            }        }        A[len] = s; // s=e        return A;    }}