package algorithm.easy;import test.foundation.S;public class BinarySearch {    public int search(int[] nums, int target) {        int high = nums.length -1 ;        int low = 0;        int mid;        while(high >= low){            mid = (high + low) / 2;            S.sop(mid, ",val:", nums[mid]);            if(nums[mid] > target){                // left                high = mid-1;            } else if(nums[mid] < target){                // right                low = mid + 1;            } else{                return mid;            }        }        return -1;    }}